version: '3.8'

services:
  bot:
    build: .
    container_name: oriental_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./main.db:/app/main.db
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"